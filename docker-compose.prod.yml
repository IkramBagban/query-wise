services:
  # Redis is hosted on Redis Cloud — no local redis service needed
  # Set QUERYWISE_REDIS_URL in .env to point to Redis Cloud
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5
  #   restart: unless-stopped

  # Web is deployed to Vercel — not needed on VM
  # web:
  #   image: ikrambagban/query-wise-web:latest
  #   env_file: .env
  #   ports:
  #     - "4100:4100"
  #   depends_on:
  #     redis:
  #       condition: service_healthy
  #   restart: unless-stopped

  worker:
    image: ikrambagban/query-wise-worker:latest
    env_file: .env
    restart: unless-stopped
