import { PlaceholderPage } from "@/components/placeholder-page";

export default function UsersPage() {
  return (
    <PlaceholderPage
      title="Users"
      description="All users with plan, quota, and cost (SPEC-08 §5.2). List and filters land in Phase 2."
    />
  );
}
