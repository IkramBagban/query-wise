import { PlaceholderPage } from "@/components/placeholder-page";

export default function CostPage() {
  return (
    <PlaceholderPage
      title="LLM cost"
      description="Token spend by provider, model, and task (SPEC-08 §5.5). Phase 4."
    />
  );
}
