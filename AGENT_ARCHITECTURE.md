hey i don't like the current approach of intent classifying with seperate call. I am actually thinking to go with an agent approach

Replace the entire current query pipeline with a constrained agent that has one tool: execute_query. Delete all intent classification, regex patterns, hardcoded replies, and multi-step LLM chains.

when user sends a message, agent will see it if the message is something that doesn't need db details, or user just asking questions but not about the db, so directly respond user, but if user asking about it's db related stuff, like which top 4 products which we sell this month by revenue etc. then call the tool. 

execute_query
This tool receives a natural language question. Internally it does:

Call generateSQL(question, schema, history) — existing function, keep as is
Call validateAndSanitizeSql(sql) — if blocked, throw error with reason
Call executeQuery(sql) — with row cap, timeout, read-only transaction
Return { sql, columns, rows, rowCount }


agent  passes the natural language question to the tool and gets data back.

The agent receives:
    - User message
    - Conversation history 
    - Schema context

write a good system prompt for agents, and tell it what to do, when to call tool, etc. also after getting the database from the tool, the agent will also decide which chart to select. you can understand context after reading [ASSIGNMENT.md](docs/ASSIGNMENT.md) and the code. 


TONE:
- Senior analyst, not a chatbot
- Concise, direct, data-focused
- Acknowledge chat naturally but keep it brief


What To Delete Entirely
- classifyIntentHeuristically — all regex patterns, gone
- classifyQuestionIntent — gone
- planAnalystTurn — gone
- generateExplanation as separate LLM call — gone
- generateChartHint as separate LLM call — gone
- buildNoResultsMessage with regex date patterns — gone
- looksLikeConstantSelect — gone
- referencesKnownTable manual check — gone
- All hardcoded fallback reply strings — gone

What To Keep Exactly As Is
- validateAndSanitizeSql() — runs inside execute_query tool, always
- executeQuery() with row cap, timeout, read-only transaction — always
- generateSQL() — runs inside execute_query tool, keep as is
- withRetry() — keep as is
- schemaCache — keep as is
- All logging — keep as is



Error Handling
All error messages should be generated by the LLM or come from actual error objects. No hardcoded strings except these two absolute fallbacks which only fire if everything else fails:

API key invalid → "Invalid API key. Please check your settings."
Rate limit → "Rate limit reached. Please wait a moment and try again."

Everything else — timeout, blocked query, no results, parse failure — the LLM writes naturally based on context.